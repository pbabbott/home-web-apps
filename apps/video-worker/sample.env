PORT=4003

# Absolute path video-worker resolves job input/output paths against.
# Job inputPath/outputPaths are always relative to, and constrained within,
# this directory. Should point at the same media root as video-api.
MEDIA_ROOT=/change/me/to/a/local/media/directory

POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DATABASE=video_api
POSTGRES_USER=video_api
POSTGRES_PASSWORD=Password123

# How often (ms) an idle worker polls for a pending job.
POLL_INTERVAL_MS=5000

# Path/binary name used to invoke ffmpeg. Must be on PATH in the container.
FFMPEG_PATH=ffmpeg

# Path/binary name used to invoke ffprobe. Must be on PATH in the container.
FFPROBE_PATH=ffprobe

# Base URL of the OpenAI-compatible AI API server (LM Studio-style) used to
# ask a vision model whether a screenshot shows a title card. No path —
# endpoints are appended by the client (src/api/ai/ai-client.ts).
AI_API_URL=http://192.168.5.142:1234

# Model name to request from AI_API_URL.
AI_MODEL=qwen/qwen3-vl-8b-instruct

# Base URL of the Sonarr instance used to look up official episode lists.
SONARR_API_URL=https://sonarr.local.abbottland.io

# Sonarr API key (Settings > General in the Sonarr UI). Populated from
# 1Password by `pnpm env:generate` (abctl.yml) — see docs/dev-env-op.md.
SONARR_API_KEY=secret_value_goes_here
