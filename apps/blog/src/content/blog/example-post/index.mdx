---
title: 'Getting Started with MDX Blog Posts'
excerpt: 'Learn how to create blog posts using MDX with frontmatter metadata. This example post demonstrates the structure and capabilities of the blog system.'
date: '2025-01-15'
categories:
  - 'Web Dev'
seoDescription: 'A comprehensive guide to writing MDX blog posts with frontmatter metadata for SEO and content management.'
seoKeywords:
  - MDX
  - Blog
  - Next.js
  - React
  - Frontmatter
status: 'draft'
---

import diagramData from './diagram.json';
import {
  Table,
  TableHead,
  TableBody,
  TableRow,
  Th,
  Td,
} from '@abbottland/fui-components';

Welcome to your first MDX blog post! This example demonstrates how to structure your blog content using MDX with frontmatter metadata.

## What is MDX?

MDX is a format that lets you seamlessly write JSX in your Markdown documents. You can import components, such as interactive charts or alerts, and embed them within your content.

### Frontmatter field usage (codebase reference)

<Table color="primary">
  <TableHead>
    <TableRow>
      <Th>Field</Th>
      <Th>Description</Th>
      <Th>Usage</Th>
    </TableRow>
  </TableHead>
  <TableBody>
    <TableRow>
      <Td>
        <strong>title</strong>
      </Td>
      <Td>The post title displayed in headings and SEO</Td>
      <Td>
        Used in Next.js `generateMetadata` (page + Open Graph),
        `BlogPostHeader`, `BlogPostCard`, `HeroBlogPostCard`, and in the blog
        listing search filter ( `BlogPageClient`).
      </Td>
    </TableRow>
    <TableRow>
      <Td>
        <strong>excerpt</strong>
      </Td>
      <Td>A brief description for post cards and previews</Td>
      <Td>
        Used in post cards (`BlogPostCard`, `HeroBlogPostCard`), in the blog
        listing search filter, and as fallback for meta description when
        `seoDescription` is missing (`[slug]/page.tsx`).
      </Td>
    </TableRow>
    <TableRow>
      <Td>
        <strong>date</strong>
      </Td>
      <Td>Publication date in YYYY-MM-DD format</Td>
      <Td>
        Used for sorting posts (newest first) in `getAllBlogPosts`, displayed in
        the post header and on cards, and in Open Graph `publishedTime`.
      </Td>
    </TableRow>
    <TableRow>
      <Td>
        <strong>categories</strong>
      </Td>
      <Td>An array of predefined categories</Td>
      <Td>
        Used to build the category filter list ( `getAllBlogCategories`), to
        filter posts by category on the blog page, and displayed as badges on
        cards and on the post header.
      </Td>
    </TableRow>
    <TableRow>
      <Td>
        <strong>status</strong>
      </Td>
      <Td>—</Td>
      <Td>
        Used in `getAllBlogPosts`: only posts with `status: 'published'` (or no
        status) are included; `draft` posts are excluded from the listing.
      </Td>
    </TableRow>
    <TableRow>
      <Td>
        <strong>seoDescription</strong>
      </Td>
      <Td>SEO meta description (optional)</Td>
      <Td>
        Used in `generateMetadata` for meta description and Open Graph
        description; falls back to `excerpt` when not set.
      </Td>
    </TableRow>
    <TableRow>
      <Td>
        <strong>seoKeywords</strong>
      </Td>
      <Td>SEO keywords array (optional)</Td>
      <Td>
        Passed to Next.js metadata `keywords` in `generateMetadata` for SEO.
      </Td>
    </TableRow>
    <TableRow>
      <Td>
        <strong>bannerImage</strong>
      </Td>
      <Td>—</Td>
      <Td>
        Filename (relative to the post folder) for the banner image; used by
        `BlogPostBannerImage` (served via `/api/blog-images/[slug]/[filename]`)
        and passed into `BlogPostCard` and `HeroBlogPostCard`; if missing, a
        gradient placeholder is shown.
      </Td>
    </TableRow>
    <TableRow>
      <Td>
        <strong>readTime</strong>
      </Td>
      <Td>Computed automatically from content (do not set in frontmatter)</Td>
      <Td>
        Not a frontmatter field; computed from content in `getBlogPostBySlug`
        with the `reading-time` library and always set on the loaded post.
      </Td>
    </TableRow>
  </TableBody>
</Table>

## Using React Components

One of the powerful features of MDX is the ability to use React components directly in your content:

```jsx
import { Button } from '@abbottland/fui-components';
<Button color="primary">Click me!</Button>;
```

## Code Blocks

You can include syntax-highlighted code blocks:

```typescript
interface BlogPost {
  title: string;
  excerpt: string;
  date: string;
  categories: BlogCategory[];
}
```

## Rendering Diagrams

You can render interactive React Flow diagrams in your blog posts. The `DiagramViewer` component is available globally in all MDX files. Just import your diagram JSON file and use it:

```jsx
import diagramData from './diagram.json';

<DiagramViewer data={diagramData} name="My Diagram" />;
```

Here's an example diagram:

<DiagramViewer data={diagramData} height="500px" name="Example Diagram" />

The diagram is interactive - you can pan and zoom to explore the nodes and connections. Diagrams are created using the diagram-maker app and exported as JSON files.

## Marking AI-Generated Content

Wrap any AI-assisted section in `<AIContent>` so readers can optionally highlight it via the
Reader Tools drawer on the left edge of the page:

<AIContent>

This paragraph was drafted with AI assistance. Toggle **Highlight AI Generated Text** in the
Reader Tools drawer to see it called out — the highlight is off by default and persists across
reloads once a reader turns it on.

</AIContent>

## Next Steps

To create a new blog post:

1. Create a new folder in `src/content/blog/` with your post slug as the folder name
2. Add an `index.mdx` file with frontmatter and content
3. The post will automatically be available at `/blog/[slug]`

Happy blogging!
