// Auto-generated file - DO NOT EDIT
// Generated by: pnpm test:playwright:generate
import { test, expect } from '@playwright/test';
import { gotoStory, STANDARD_VIEWPORT, SCREENSHOT_OPTIONS } from '<%= helpersPath %>';

test.describe('<%= componentName %> Visual Regression Tests', () => {
  test.beforeEach(async ({ page }) => {
    await page.setViewportSize(STANDARD_VIEWPORT);
  });

<% tests.forEach(function(testCase) { %>
  test('<%- testCase.name %>', async ({ page }) => {
    await gotoStory(page, '<%- testCase.storyId %>');
    await expect(page.locator('#storybook-root')).toHaveScreenshot('<%- testCase.screenshotName %>', SCREENSHOT_OPTIONS);
  });

<% }); %>
});

