// Auto-generated file - DO NOT EDIT
// Generated by: pnpm test:playwright:generate
import { test, expect } from '@playwright/test';
import {
  gotoStory,
  STANDARD_VIEWPORT,
  SCREENSHOT_OPTIONS,
} from '../../../tests/playwright-helpers';

test.describe('Modal Visual Regression Tests', () => {
  test.beforeEach(async ({ page }) => {
    await page.setViewportSize(STANDARD_VIEWPORT);
  });

  test('primary', async ({ page }) => {
    await gotoStory(page, 'components-modal--primary');
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'components-modal--primary.png',
      SCREENSHOT_OPTIONS,
    );
  });

  test('secondary', async ({ page }) => {
    await gotoStory(page, 'components-modal--secondary');
    await expect(page.locator('#storybook-root')).toHaveScreenshot(
      'components-modal--secondary.png',
      SCREENSHOT_OPTIONS,
    );
  });
});
